import { GenericOption } from "../../settings/types";

// Interface defining the structure for color theme settings
export interface ColorThemeSettings {
  options?: GenericOption[];
  default?: GenericOption;
}
